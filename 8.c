#include<stdio.h>
void main()
{
   int n,count,ev,od;
   count=0;
   printf("enter the number n=");
   scanf("%d",&n);
   while(n!=0)
   {
     rem=n%10;
     if(n%2==0)
     {
       
       ev=ev+count;
       printf("%d",es);
       count++;
     }
      else
     {
       od=od+count;
       printf("%d",od);
       count++;
     }
    }
}
   